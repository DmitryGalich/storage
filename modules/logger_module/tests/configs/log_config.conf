* GLOBAL:
   FORMAT               =  "%datetime %level %msg"
   FILENAME             =  "/home/dmitry/Projects/logs/loggger_module_tests.log" 
   ## I had some problems with relative path. But in apps folder relative path "logs/server.log" works fine and create log folder right next to binary file
   ENABLED              =  true
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  true
   PERFORMANCE_TRACKING =  true
   MAX_LOG_FILE_SIZE    =  2097152 ## 2MB - Comment starts with two hashes (##)
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs
* DEBUG:
   FORMAT               =  "%datetime %level [ %fbase %line %func ] %msg"
